/// A trait for a builder; this represents a constructor within our DI system.
pub mod builder;
/// A trait for retrieving inputs (dependencies) for DI objects.
pub mod get_input;
/// Trait for providing inputs (dependencies) to a builder.
pub mod input_provider;
